form.form.form_login(ua-login)
    .form__title.form__title_center Sign in to continue
    .form__row
      img.form__img(src="img/profile.png")
    .form__row
        input.form__input(placeholder="Username", name="login", type="text", autofocus)
    .form__row
        input.form__input(placeholder="Password", name="password", type="password")
    .form__row
        button.btn.btn_block.hvr-ripple-out(type="submit") Sign in
    .form__footer
      p.form__error(ng-show="error") {{ error.message }}
      .form__info
        | Don't have an account?
        a.form__link(ui-sref="signup") Sign Up Here
    .loading-bar(ng-show="loading")
      img.loading-bar__img(src="img/ajax-loader.gif")
